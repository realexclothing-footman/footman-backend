const { sequelize } = require('./src/config/database');
const Request = require('./src/models/Request');

async function syncRequests() {
  try {
    await Request.sync({ alter: true });
    console.log('✅ Requests table synced successfully!');
    console.log('📊 Simple request system ready:');
    console.log('   - 50 BDT for ≤1.5KM');
    console.log('   - 100 BDT for 1.5-3KM');
    console.log('   - 10% platform commission');
    process.exit(0);
  } catch (error) {
    console.error('❌ Error syncing requests:', error.message);
    process.exit(1);
  }
}

syncRequests();
